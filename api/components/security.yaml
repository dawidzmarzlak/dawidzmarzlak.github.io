# Security schemes

securitySchemes:
  bearerAuth:
    type: http
    scheme: bearer
    bearerFormat: JWT
    description: |
      JWT Bearer token authentication.

      Obtain token via POST /api/v1/auth/login

      Include in header:
      ```
      Authorization: Bearer <token>
      ```
